// Add at the top of the file with other exports or type definitions
export interface SchemaColumn {
  name: string;
  type: 'string' | 'text' | 'number' | 'boolean' | 'date' | 'object' | 'array' | 'unknown';
}

// The rest of the file would remain unchanged
// Since I don't have the full content of useNodeManagement.ts, I can't provide the complete file
// You would need to add the SchemaColumn interface to your existing file
